# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password
DB_DATABASE=job_offers_db
DB_SYNCHRONIZE=false  # Set to false in production
DB_LOGGING=true # Set to false in production

# API URLs
API_1_URL=http://api1.example.com/jobs
API_2_URL=http://api2.example.com/jobs

# Cron Job Configuration
CRON_JOB_SCHEDULE=0 * * * *  # Example: Run every hour.  Adjust as needed.

# Other configurations
PORT=3000